<UserControl x:Class="SimpleOverlayEditor.Views.TemplateEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SimpleOverlayEditor.ViewModels"
             xmlns:models="clr-namespace:SimpleOverlayEditor.Models"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             Focusable="True"
             PreviewKeyDown="Root_PreviewKeyDown">
    <UserControl.InputBindings>
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding UndoCommand, FallbackValue={x:Null}}"/>
    </UserControl.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" MinWidth="180"/>  <!-- 왼쪽: 정보 패널 -->
            <ColumnDefinition Width="5"/>  <!-- Splitter -->
            <ColumnDefinition Width="*"/>    <!-- 중앙: 이미지 뷰 -->
            <ColumnDefinition Width="5"/>  <!-- Splitter -->
            <ColumnDefinition Width="180" MinWidth="150"/>  <!-- 오른쪽: 샘플 이미지 정보 -->
        </Grid.ColumnDefinitions>

        <!-- 왼쪽: 정보 패널 -->
        <DockPanel Grid.Column="0" Background="#F5F5F5">
            <TextBlock DockPanel.Dock="Top" 
                      Text="템플릿 편집" 
                      FontWeight="Bold" 
                      FontSize="14"
                      Margin="10,10,10,5"
                      Padding="5"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <!-- 오버레이 목록 -->
                    <GroupBox Header="오버레이 목록" Margin="0,0,0,10">
                        <DataGrid x:Name="OverlayDataGrid"
                                 ItemsSource="{Binding CurrentOverlayCollection}"
                                 AutoGenerateColumns="False"
                                 CanUserAddRows="False"
                                 CanUserDeleteRows="False"
                                 SelectionMode="Extended"
                                 SelectionUnit="FullRow"
                                 HeadersVisibility="Column"
                                 LoadingRow="DataGrid_LoadingRow"
                                 SelectionChanged="DataGrid_SelectionChanged"
                                 MaxHeight="200"
                                 EnableRowVirtualization="False">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <!-- 기본 스타일: 흰색 배경, 테두리 없음 -->
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    
                                    <Style.Triggers>
                                        <!-- 호버 효과 (선택되지 않은 행만) -->
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                                <Condition Property="IsSelected" Value="False"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="#F0F0F0"/>
                                        </MultiTrigger>
                                        
                                        <!-- 선택된 행: 캔버스와 동일한 파란색 계열 -->
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#E3F2FD"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                        
                                        <!-- 선택 + 호버 -->
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="#BBDEFB"/>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <!-- 셀이 시스템 Highlight로 칠하지 못하게 막기 -->
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.RowHeaderStyle>
                                <Style TargetType="DataGridRowHeader">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                    <!-- DataTrigger 제거: RelativeSource로 인한 null 키 문제 방지 -->
                                    <!-- RowStyle에서 이미 선택 상태를 처리하므로 중복 제거 -->
                                </Style>
                            </DataGrid.RowHeaderStyle>
                            <DataGrid.Columns>
                                <!-- 번호 컬럼: OptionNumber (IdentityIndex) 표시 -->
                                <DataGridTextColumn Header="번호" Binding="{Binding OptionNumber, FallbackValue='-'}" Width="50" IsReadOnly="True"/>
                                <DataGridTextColumn Header="타입" Binding="{Binding OverlayType, FallbackValue=''}" Width="80"/>
                                <DataGridTextColumn Header="X" Binding="{Binding X, StringFormat=F1, FallbackValue=0}" Width="50"/>
                                <DataGridTextColumn Header="Y" Binding="{Binding Y, StringFormat=F1, FallbackValue=0}" Width="50"/>
                                <DataGridTextColumn Header="W" Binding="{Binding Width, StringFormat=F1, FallbackValue=0}" Width="50"/>
                                <DataGridTextColumn Header="H" Binding="{Binding Height, StringFormat=F1, FallbackValue=0}" Width="50"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>

                    <!-- 선택된 오버레이 상세 편집 -->
                    <GroupBox Header="선택된 사각형 편집" Margin="0,0,0,10">
                        <StackPanel Margin="5" IsEnabled="{Binding SelectionVM.IsEmpty, Converter={StaticResource InverseBoolToBoolConverter}, FallbackValue=True}">
                            <!-- 다중 선택 표시 -->
                            <TextBlock Text="{Binding SelectionVM.Count, StringFormat='선택: {0}개'}"
                                      FontWeight="Bold"
                                      Margin="0,0,0,5"
                                      Visibility="{Binding SelectionVM.IsMulti, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"/>
                            
                            <TextBlock Text="X:" Margin="0,5,0,2"/>
                            <TextBox x:Name="XTextBox"
                                    Text="{Binding SelectionVM.XDisplay, Mode=OneWay}"
                                    GotKeyboardFocus="NumericTextBox_GotKeyboardFocus"
                                    LostFocus="NumericTextBox_LostFocus"
                                    KeyDown="NumericTextBox_KeyDown"
                                    Margin="0,0,0,5"/>
                            <TextBlock Text="Y:" Margin="0,5,0,2"/>
                            <TextBox x:Name="YTextBox"
                                    Text="{Binding SelectionVM.YDisplay, Mode=OneWay}"
                                    GotKeyboardFocus="NumericTextBox_GotKeyboardFocus"
                                    LostFocus="NumericTextBox_LostFocus"
                                    KeyDown="NumericTextBox_KeyDown"
                                    Margin="0,0,0,5"/>
                            <TextBlock Text="너비 (W):" Margin="0,5,0,2"/>
                            <TextBox x:Name="WidthTextBox"
                                    Text="{Binding SelectionVM.WidthDisplay, Mode=OneWay}"
                                    GotKeyboardFocus="NumericTextBox_GotKeyboardFocus"
                                    LostFocus="NumericTextBox_LostFocus"
                                    KeyDown="NumericTextBox_KeyDown"
                                    Margin="0,0,0,5"/>
                            <TextBlock Text="높이 (H):" Margin="0,5,0,2"/>
                            <TextBox x:Name="HeightTextBox"
                                    Text="{Binding SelectionVM.HeightDisplay, Mode=OneWay}"
                                    GotKeyboardFocus="NumericTextBox_GotKeyboardFocus"
                                    LostFocus="NumericTextBox_LostFocus"
                                    KeyDown="NumericTextBox_KeyDown"
                                    Margin="0,0,0,5"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- 정렬 도구 -->
                    <GroupBox Header="정렬" Margin="0,0,0,10">
                        <StackPanel Margin="5">
                            <Button Content="왼쪽에 맞추기" Command="{Binding AlignLeftCommand, FallbackValue={x:Null}}" Margin="2" Padding="6,3" FontSize="10"/>
                            <Button Content="위쪽에 맞추기" Command="{Binding AlignTopCommand, FallbackValue={x:Null}}" Margin="2" Padding="6,3" FontSize="10"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- 사용법 안내 -->
                    <GroupBox Header="사용법" Margin="0,0,0,10">
                        <StackPanel Margin="5">
                            <TextBlock TextWrapping="Wrap" FontSize="10" LineHeight="18">
                                <Run Text="키보드 단축키:" FontWeight="Bold"/>
                                <LineBreak/>
                                <Run Text="• 방향키: 위치 조정 (1px)"/>
                                <LineBreak/>
                                <Run Text="• Shift + 방향키: 크기 조정 (1px)"/>
                                <LineBreak/>
                            </TextBlock>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>

        <!-- 왼쪽 Splitter -->
        <GridSplitter Grid.Column="1"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     Width="5"
                     Background="#E0E0E0"
                     ShowsPreview="False"
                     ResizeDirection="Columns"/>

        <!-- 중앙: 이미지 뷰 + 도구바 -->
        <DockPanel Grid.Column="2">
            <!-- 상단 도구바 -->
            <StackPanel DockPanel.Dock="Top" 
                       Orientation="Horizontal" 
                       Background="#E8E8E8"
                       Margin="3"
                       Height="36">
                <ComboBox SelectedItem="{Binding CurrentOverlayType, Mode=TwoWay, FallbackValue={x:Null}}"
                         Margin="3,0"
                         VerticalAlignment="Center"
                         MinWidth="100"
                         FontSize="11">
                    <ComboBox.ItemsSource>
                        <x:Array Type="{x:Type models:OverlayType}">
                            <models:OverlayType>TimingMark</models:OverlayType>
                            <models:OverlayType>ScoringArea</models:OverlayType>
                            <models:OverlayType>BarcodeArea</models:OverlayType>
                        </x:Array>
                    </ComboBox.ItemsSource>
                </ComboBox>
                <Separator Margin="3,0"/>
                <TextBlock Text="문항:" 
                          VerticalAlignment="Center" 
                          Margin="3,0" 
                          FontSize="11"
                          Visibility="{Binding IsQuestionNumberVisible, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"/>
                <ComboBox SelectedItem="{Binding CurrentQuestionNumber, Mode=TwoWay, FallbackValue={x:Null}}"
                         Margin="3,0"
                         VerticalAlignment="Center"
                         MinWidth="60"
                         FontSize="11"
                         Visibility="{Binding IsQuestionNumberVisible, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
                    <ComboBox.ItemsSource>
                        <Binding Path="QuestionNumbers"/>
                    </ComboBox.ItemsSource>
                </ComboBox>
                <Separator Margin="5,0"/>
                <Button Content="템플릿 저장" 
                       Command="{Binding SaveTemplateCommand, FallbackValue={x:Null}}"
                       Margin="2"
                       Padding="6,3"
                       MinWidth="80"
                       Background="#4CAF50"
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="11"/>
                <Separator Margin="5,0"/>
                <Button Content="템플릿 내보내기" 
                       Command="{Binding TemplateViewModel.ExportTemplateCommand, FallbackValue={x:Null}}"
                       Margin="2"
                       Padding="6,3"
                       MinWidth="100"
                       Background="#2196F3"
                       Foreground="White"
                       FontSize="11"
                       ToolTip="현재 템플릿을 JSON 파일로 내보냅니다."/>
                <Button Content="템플릿 가져오기" 
                       Command="{Binding TemplateViewModel.ImportTemplateCommand, FallbackValue={x:Null}}"
                       Margin="2"
                       Padding="6,3"
                       MinWidth="100"
                       Background="#FF9800"
                       Foreground="White"
                       FontSize="11"
                       ToolTip="JSON 파일에서 템플릿을 가져옵니다."/>
            </StackPanel>

            <!-- 이미지 캔버스 -->
            <ScrollViewer x:Name="ImageScrollViewer"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         Background="#E0E0E0"
                         VerticalContentAlignment="Top"
                         PreviewMouseWheel="ImageScrollViewer_PreviewMouseWheel">
                <Grid x:Name="ImageContainer"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      SizeChanged="ImageContainer_SizeChanged">
                    <Canvas x:Name="ImageCanvas" 
                           MouseLeftButtonDown="ImageCanvas_MouseLeftButtonDown"
                           MouseMove="ImageCanvas_MouseMove"
                           MouseLeftButtonUp="ImageCanvas_MouseLeftButtonUp"
                           MouseLeave="ImageCanvas_MouseLeave"
                           Background="White"
                           SizeChanged="ImageCanvas_SizeChanged"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Left">
                        <Image x:Name="SourceImage" 
                              Source="{Binding SelectedDocument.SourcePath, FallbackValue={x:Null}}"
                              Stretch="Uniform"
                              RenderOptions.BitmapScalingMode="HighQuality"
                              IsHitTestVisible="False"/>
                        <!-- 오버레이는 코드비하인드에서 동적으로 그리기 -->
                    </Canvas>
                    
                    <!-- 이미지가 없을 때 표시할 안내 메시지 -->
                    <Border x:Name="NoImageHint"
                            Background="#F5F5F5"
                            BorderBrush="#CCCCCC"
                            BorderThickness="1"
                            CornerRadius="5"
                            Padding="20"
                            Visibility="Collapsed"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="📷" 
                                      FontSize="48" 
                                      HorizontalAlignment="Center" 
                                      Margin="0,0,0,10"/>
                            <TextBlock Text="이미지를 먼저 로드해주세요" 
                                      FontSize="14" 
                                      FontWeight="Bold"
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,5"/>
                            <TextBlock Text="오른쪽 패널의 '이미지 로드' 버튼을 클릭하세요" 
                                      FontSize="11" 
                                      Foreground="#666666"
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </ScrollViewer>
        </DockPanel>

        <!-- 오른쪽 Splitter -->
        <GridSplitter Grid.Column="3"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     Width="5"
                     Background="#E0E0E0"
                     ShowsPreview="False"
                     ResizeDirection="Columns"/>

        <!-- 오른쪽: 샘플 이미지 정보 -->
        <DockPanel Grid.Column="4" Background="#F5F5F5">
            <StackPanel DockPanel.Dock="Top" Margin="10">
                <TextBlock Text="샘플 이미지" 
                          FontWeight="Bold" 
                          FontSize="14"
                          Margin="0,10,0,10"/>
                <Button Content="홈으로" 
                       Command="{Binding NavigateToHomeCommand, FallbackValue={x:Null}}"
                       Style="{StaticResource CleanButton}"
                       Background="#616161"
                       Margin="0,0,0,10"/>
                <Button Content="이미지 로드" 
                       Command="{Binding LoadSampleImageCommand, FallbackValue={x:Null}}"
                       Style="{StaticResource CleanButton}"
                       Background="#4CAF50"
                       Margin="0,0,0,10"/>
            </StackPanel>
            <StackPanel Margin="10">
                <GroupBox Header="이미지 정보" Margin="0,0,0,10">
                    <StackPanel Margin="5" IsEnabled="{Binding SelectedDocument, Converter={StaticResource NullToBoolConverter}, FallbackValue=False}">
                        <TextBlock Margin="0,5,0,2" TextWrapping="Wrap">
                            <Run Text="파일명:" FontWeight="Bold"/>
                            <LineBreak/>
                            <Run Text="{Binding SelectedDocument.SourcePath, Converter={StaticResource FileNameConverter}, FallbackValue=''}" 
                                 FontSize="11"/>
                        </TextBlock>
                        <TextBlock Margin="0,10,0,2">
                            <Run Text="너비:" FontWeight="Bold"/>
                            <Run Text="{Binding SelectedDocument.ImageWidth, FallbackValue=0}"/> <Run Text="px"/>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,2">
                            <Run Text="높이:" FontWeight="Bold"/>
                            <Run Text="{Binding SelectedDocument.ImageHeight, FallbackValue=0}"/> <Run Text="px"/>
                        </TextBlock>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>








