<UserControl x:Class="SimpleOverlayEditor.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 상단 타이틀 -->
        <Border Grid.Row="0" 
                Background="#2196F3" 
                Padding="20,30">
            <TextBlock Text="OMR 오버레이 에디터" 
                      FontSize="32" 
                      FontWeight="Bold" 
                      Foreground="White" 
                      HorizontalAlignment="Center"/>
        </Border>

        <!-- 메인 콘텐츠 -->
        <StackPanel Grid.Row="1" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center" 
                   Margin="40">
            <TextBlock Text="작업 모드를 선택하세요" 
                      FontSize="18" 
                      Foreground="#666" 
                      HorizontalAlignment="Center" 
                      Margin="0,0,0,40"/>

            <!-- 템플릿 편집 버튼 -->
            <Button Content="템플릿 편집" 
                   Command="{Binding NavigateToTemplateEditCommand}"
                   Style="{StaticResource HomeModeButton}"/>

            <!-- 마킹 리딩 버튼 -->
            <Button Content="마킹 리딩" 
                   Command="{Binding NavigateToMarkingCommand}"
                   Style="{StaticResource HomeModeButton}"/>

            <!-- 수험생 명렬 관리 버튼 -->
            <Button Content="수험생 명렬 관리" 
                   Command="{Binding NavigateToRegistryCommand}"
                   Style="{StaticResource HomeModeButton}"/>
        </StackPanel>
    </Grid>
</UserControl>








