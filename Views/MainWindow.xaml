<Window x:Class="SimpleOverlayEditor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SimpleOverlayEditor.ViewModels"
        xmlns:views="clr-namespace:SimpleOverlayEditor.Views"
        mc:Ignorable="d"
        Title="OMR Overlay Editor" Width="1400" Height="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- 모드에 따른 View 데이터 템플릿 -->
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TemplateEditViewModel}">
            <views:TemplateEditView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MarkingViewModel}">
            <views:MarkingView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RegistryViewModel}">
            <views:RegistryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GradingViewModel}">
            <views:GradingView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScoringRuleViewModel}">
            <views:ScoringRuleView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ManualVerificationViewModel}">
            <views:ManualVerificationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SingleStudentVerificationViewModel}">
            <views:SingleStudentVerificationView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <!-- ContentControl: 현재 모드에 따라 View 전환 -->
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
