<UserControl x:Class="SimpleOverlayEditor.Views.GradingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 상단 헤더 -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="채점 및 성적 처리" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          VerticalAlignment="Center"/>
                <Button Grid.Column="1"
                       Content="홈으로"
                       Command="{Binding NavigateToHomeCommand}"
                       Padding="15,5"
                       Background="#757575"
                       Foreground="White"
                       FontWeight="Normal"
                       Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <!-- 수험번호 불일치 경고 (DataGrid 위) -->
        <Border Grid.Row="1"
                Background="#FFF3CD"
                BorderBrush="#FFC107"
                BorderThickness="0,0,0,2"
                Padding="15,10"
                Margin="10,10,10,0"
                Visibility="{Binding HasMismatch, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="⚠ 수험번호 불일치 발견" 
                           FontWeight="Bold" 
                           FontSize="14"
                           Foreground="#856404"
                           Margin="0,0,0,8"/>
                <TextBlock Text="{Binding MismatchMessage}"
                           Foreground="#856404"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           Margin="0,0,0,5"/>
                <StackPanel Margin="10,0,0,0">
                    <TextBlock Text="{Binding MissingInGradingList}"
                               Foreground="#856404"
                               TextWrapping="Wrap"
                               Visibility="{Binding MissingInGradingList, Converter={StaticResource NullToVisibilityConverter}}"/>
                    <TextBlock Text="{Binding MissingInRegistryList}"
                               Foreground="#856404"
                               TextWrapping="Wrap"
                               Margin="0,3,0,0"
                               Visibility="{Binding MissingInRegistryList, Converter={StaticResource NullToVisibilityConverter}}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredGradingResults}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F5F5F5"
                  Margin="10"
                  Sorting="GradingDataGrid_Sorting">
            <DataGrid.Columns>
                <DataGridTextColumn Header="수험번호" Binding="{Binding StudentId}" SortMemberPath="StudentId" Width="100"/>
                <DataGridTextColumn Header="접수번호" Binding="{Binding RegistrationNumber}" SortMemberPath="RegistrationNumber" Width="100"/>
                <DataGridTextColumn Header="전형명" Binding="{Binding ExamType}" SortMemberPath="ExamType" Width="100"/>
                <DataGridTextColumn Header="성명" Binding="{Binding StudentName}" SortMemberPath="StudentName" Width="100"/>
                <DataGridTextColumn Header="생년월일" Binding="{Binding BirthDate}" SortMemberPath="BirthDate" Width="120"/>
                <DataGridTextColumn Header="출신교명" Binding="{Binding MiddleSchool}" SortMemberPath="MiddleSchool" Width="150"/>
                <DataGridTextColumn Header="문항1" Binding="{Binding Question1Marking}" SortMemberPath="Question1Marking" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="문항2" Binding="{Binding Question2Marking}" SortMemberPath="Question2Marking" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="문항3" Binding="{Binding Question3Marking}" SortMemberPath="Question3Marking" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="문항4" Binding="{Binding Question4Marking}" SortMemberPath="Question4Marking" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="총점" Binding="{Binding TotalScore}" SortMemberPath="TotalScore" Width="70">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="석차" Binding="{Binding Rank}" SortMemberPath="Rank" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridCheckBoxColumn Header="중복" Binding="{Binding IsDuplicate}" SortMemberPath="IsDuplicate" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="중복수" Binding="{Binding DuplicateCount}" SortMemberPath="DuplicateCount" Width="70">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridCheckBoxColumn Header="오류" Binding="{Binding HasErrors}" SortMemberPath="HasErrors" Width="60" IsReadOnly="True"/>
            </DataGrid.Columns>

            <!-- 중복 및 오류 행을 색깔로 표시 -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <!-- 중복과 오류가 모두 있는 경우: 진한 빨간색 -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsDuplicate}" Value="True"/>
                                <Condition Binding="{Binding HasErrors}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Background" Value="#E57373"/>
                            <Setter Property="ToolTip" Value="중복 및 오류가 모두 있습니다."/>
                        </MultiDataTrigger>
                        <!-- 오류만 있는 경우: 주황색 -->
                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                            <Setter Property="Background" Value="#FFB74D"/>
                            <Setter Property="ToolTip" Value="오류가 있습니다."/>
                        </DataTrigger>
                        <!-- 중복만 있는 경우: 연한 빨간색 -->
                        <DataTrigger Binding="{Binding IsDuplicate}" Value="True">
                            <Setter Property="Background" Value="#FFCDD2"/>
                            <Setter Property="ToolTip" Value="같은 수험번호+면접번호 조합으로 여러 시트가 존재합니다. (중복)"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</UserControl>
